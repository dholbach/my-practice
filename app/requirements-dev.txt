# Kept in sync with requirements.txt by hand (no `-r` include) so Dependabot
# can patch this file's pins directly — it doesn't resolve `-r` includes when
# generating security-update PRs, which left every runtime CVE alert stuck
# against this file with no way for Dependabot to fix it.
#
# scripts/check_requirements_sync.py enforces the duplication: every pin in
# requirements.txt must appear here at the same version. Extra entries here
# (the tooling below) are expected and never reach the production image.
Django==6.1.1
psycopg[binary]==3.3.6
cryptography==50.0.1
markdown==3.10.3
nh3==0.3.7
gunicorn==26.2.0
python-dotenv==1.2.3
weasyprint==70.0
pydyf==0.12.1
pillow==12.3.0
whitenoise==6.12.0
python-dateutil==2.9.0.post0
pdfplumber==0.11.10
pypdf==6.19.0
urllib3==2.8.0
sqlparse==0.6.0  # transitive via Django (>=0.5.0); pinned to close PYSEC-2026-3696..3699

# Google Calendar API
google-auth==2.58.0
google-auth-oauthlib==1.4.1
google-auth-httplib2==0.4.2
google-api-python-client==2.200.0

# Dev-only tools
ruff==0.16.8
pre-commit==4.6.2
vulture==2.16
radon==6.0.1
pip-audit==2.10.1
pytest==9.1.1
pytest-django==4.14.0
pytest-testmon==2.2.0
mypy==2.3.1
django-stubs==6.1.1
types-python-dateutil==2.9.0.20260807
types-Markdown==3.10.2.20260712
